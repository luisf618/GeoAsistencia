name: geoasistencia_app
description: GeoAsistencia MVP - App móvil para registro de asistencia con validación por geocerca (Semana 3).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0
  geolocator: ^10.1.0
  permission_handler: ^11.3.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
